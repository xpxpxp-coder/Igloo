{
  "name": "snowman-command-center-workspace",
  "private": true,
  "packageManager": "pnpm@11.4.0",
  "scripts": {
    "brand:generate": "node scripts/generate-snowman-product.mjs",
    "brand:check": "node scripts/generate-snowman-product.mjs --check",
    "brand:surfaces": "node scripts/check-snowman-brand.mjs",
    "check": "pnpm brand:check && pnpm brand:surfaces && pnpm -r check"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.6"
  },
  "dependencies": {
    "@tailwindcss/typography": "^0.5.19"
  }
}
