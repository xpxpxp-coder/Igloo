name: snowman-command-center

services:
  postgres:
    image: postgres:17-alpine
    container_name: snowman-command-center-postgres
    environment:
      POSTGRES_USER: buzz
      POSTGRES_PASSWORD: buzz_dev
      POSTGRES_DB: buzz
      PGDATA: /var/lib/postgresql/data
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    networks:
      - snowman-command-center-net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U buzz"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s
    deploy:
      resources:
        limits:
          memory: 512m
    labels:
      ai.snowman.service: "postgres"
      ai.snowman.environment: "development"
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    container_name: snowman-command-center-redis
    ports:
      - "6379:6379"
    networks:
      - snowman-command-center-net
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 5s
    deploy:
      resources:
        limits:
          memory: 128m
    labels:
      ai.snowman.service: "redis"
      ai.snowman.environment: "development"
    restart: unless-stopped

  adminer:
    image: adminer:latest
    container_name: snowman-command-center-adminer
    ports:
      - "8082:8080"
    networks:
      - snowman-command-center-net
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      ADMINER_DEFAULT_SERVER: postgres
    deploy:
      resources:
        limits:
          memory: 64m
    labels:
      ai.snowman.service: "adminer"
      ai.snowman.environment: "development"
    restart: unless-stopped

  keycloak:
    image: quay.io/keycloak/keycloak:26.0
    container_name: snowman-command-center-keycloak
    command: start-dev --http-port=8080
    environment:
      KC_DB: dev-mem
      KEYCLOAK_ADMIN: admin
      KEYCLOAK_ADMIN_PASSWORD: admin
    ports:
      - "8180:8080"
    networks:
      - snowman-command-center-net
    healthcheck:
      test: ["CMD-SHELL", "exec 3<>/dev/tcp/localhost/8080 && echo -e 'GET /health/ready HTTP/1.1\\r\\nHost: localhost\\r\\nConnection: close\\r\\n\\r\\n' >&3 && cat <&3 | grep -q '200 OK'"]
      interval: 10s
      timeout: 5s
      retries: 15
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
    labels:
      ai.snowman.service: "keycloak"
      ai.snowman.environment: "development"
    restart: unless-stopped

  minio:
    image: minio/minio:latest
    container_name: snowman-command-center-minio
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: buzz_dev
      MINIO_ROOT_PASSWORD: buzz_dev_secret
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio-data:/data
    networks:
      - snowman-command-center-net
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s
    deploy:
      resources:
        limits:
          memory: 256m
    labels:
      ai.snowman.service: "minio"
      ai.snowman.environment: "development"
    restart: unless-stopped

  minio-init:
    image: minio/mc:latest
    container_name: snowman-command-center-minio-init
    depends_on:
      minio:
        condition: service_healthy
    networks:
      - snowman-command-center-net
    entrypoint: >
      /bin/sh -c "
        mc alias set local http://minio:9000 buzz_dev buzz_dev_secret &&
        mc mb --ignore-existing local/buzz-media &&
        mc anonymous set none local/buzz-media
      "
    labels:
      ai.snowman.service: "minio-init"
      ai.snowman.environment: "development"
    restart: "no"

  prometheus:
    image: prom/prometheus:latest
    container_name: snowman-command-center-prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus-data:/prometheus
    networks:
      - snowman-command-center-net
    extra_hosts:
      - "host.docker.internal:host-gateway"
    deploy:
      resources:
        limits:
          memory: 128m
    labels:
      ai.snowman.service: "prometheus"
      ai.snowman.environment: "development"
    restart: unless-stopped

volumes:
  postgres-data:
    name: snowman-command-center-postgres-data
    labels:
      ai.snowman.volume: "postgres"
  minio-data:
    name: snowman-command-center-minio-data
    labels:
      ai.snowman.volume: "minio"
  prometheus-data:
    name: snowman-command-center-prometheus-data
    labels:
      ai.snowman.volume: "prometheus"

networks:
  snowman-command-center-net:
    name: snowman-command-center-net
    driver: bridge
    labels:
      ai.snowman.network: "development"
